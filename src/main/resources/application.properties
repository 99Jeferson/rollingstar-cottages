# Application Details
spring.application.name=RollingStarCottages

# PostgreSQL Database Connection Configuration - UPDATED TO MATCH SITE DB
spring.datasource.url=jdbc:postgresql://localhost:5432/rollingstar_db
spring.datasource.username=jeff_dev
spring.datasource.password=,3gGUsT.G@7j,Gi@26
spring.datasource.driver-class-name=org.postgresql.Driver

# \uD83D\uDEE0\uFE0F Turn off Thymeleaf templates caching for development
spring.thymeleaf.cache=false

# Force Hibernate to refresh entity data rather than serving old session snapshots
spring.jpa.properties.hibernate.cache.use_second_level_cache=false

# JPA / Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# 'update' automatically updates your database tables to match your Java models on startup
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080